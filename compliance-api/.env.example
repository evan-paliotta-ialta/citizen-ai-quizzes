# Copy to .env in this same directory. Never commit .env itself (already
# gitignored at the repo root via the blanket `.env` / `*.env` rule).

# Same Compliance API key already provisioned for usage_report/ in the
# "AI Policies/Claude Code Enterprise Deployment" repo (read:compliance_*
# scope, Primary Owner). Copy the value over — don't provision a second key.
CLAUDE_COMPLIANCE_API_KEY=
